<Window x:Class="FakeAtlas.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ce ="clr-namespace:FakeAtlas.CustomUIElements"
        xmlns:local="clr-namespace:FakeAtlas.Views" 
        xmlns:viewmodels="clr-namespace:FakeAtlas.ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel}"
        mc:Ignorable="d"
        Title="LoginWindow" Height="478" Width="374"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        x:Name="signUpPage">
    <WindowChrome.WindowChrome>
        <WindowChrome/>

    </WindowChrome.WindowChrome>
    <Border Background="#FF3A3F52">
        <Grid x:Name="WindowGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="41*"/>
                <RowDefinition Height="198*" />
            </Grid.RowDefinitions>
            <TextBlock Text="Fake Atlas" Foreground="#FF6397C5" FontFamily="Segoe Script" FontSize="30" Margin="103,10,103,31"/>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="328,6,4,54">
                <Button Style="{DynamicResource MinimizeButton}" Command="{Binding MinimizeCommand}" WindowChrome.IsHitTestVisibleInChrome="True" Margin="4 3 3 2" Height="18"/>
                <Button Style="{DynamicResource CloseButton}" Command="{Binding CloseCommand}" WindowChrome.IsHitTestVisibleInChrome="True" Height="18" Width="17" RenderTransformOrigin="0.5,0.5"/>
            </StackPanel>
            <TextBox Grid.Row="1" Style="{DynamicResource InputTextBox}" Text="{Binding SelectedAccount.UserLogin, UpdateSourceTrigger=PropertyChanged}" Margin="23,108,20,253"/>
            <PasswordBox Grid.Row="1" x:Name="tbPasswordBox" Style="{DynamicResource InputPasswordBox}" Margin="23,157,20,203"/>
            <Button Grid.Row="1" Style="{DynamicResource InputButton}" Command="{Binding SignInCommand}" Content="{DynamicResource m_SignIn}" Margin="23,284,21,71"/>
            <Button Content="{DynamicResource m_SignUp}" Style="{DynamicResource SignUpButton}" Command="{Binding ShowSignUpCommand}" Grid.Row="1" Margin="110,330,107,47" Background="#FF4169C7"/>
        </Grid>
    </Border>
</Window>
